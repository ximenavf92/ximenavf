<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/assets/css/index.css' | url }}">
    <link rel="stylesheet" href="{{ '/assets/css/prism-base16-monokai.dark.css' | url }}">
    <link rel="stylesheet" href="https://use.typekit.net/gct2mqj.css">
    <link rel="shortcut icon" href="/assets/img/favicon.png" type="image/x-icon">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">
  </head>
  <body>
    <div class="wrapper">
      <a class="skip-main" href="#main">Skip to Main Content</a>
      <header>
        <nav class="main-nav">
        <h1><a href="{{ '/' | url }}"><img src="/assets/img/horizontal-logo.svg" alt="" width="200" height="65" class="logo"><span class="visually-hidden">{{ metadata.title }}</span></a></h1>
  
        {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
          <ul class="nav">
            <li class="nav-item"><a href="/assets/files/XimenaVilaFerral-Resume-2021.pdf" target="_blank" rel="noopener">resume</a></li>
          {%- for entry in collections.all | eleventyNavigation %}
            <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title | lowercase }}</a></li>
          {%- endfor %}
          </ul>
        </nav>
        <nav class="mobile-nav">
          <ul class="nav">
            <li class="nav-item nav-item-home-icon"><a href="/"><span class="visually-hidden">home</span><img src="/assets/img/favicon.png" alt="" width="22px"></a></li>
            <li class="nav-item"><a href="/assets/files/XimenaVilaFerral-Resume-2021.pdf" target="_blank" rel="noopener">resume</a></li>
            {%- for entry in collections.all | eleventyNavigation %}
              <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title | lowercase }}</a></li>
            {%- endfor %}
            </ul>
        </nav>
      </header>
  
      <main id="main" {%if templateClass %} class="{{ templateClass }}"{% endif %}>
        {{ content | safe }}
        
      </main>
  
      <footer class="main-footer">
        <ul class="social-links">
          {% for social in metadata.socials %}
          <li class="social-links-item social-links-{{ social.name }}">
            <a href="{{ social.link }}" target="_blank" rel="noopener">
              <img src="{{ social.icon }}" alt="" width="20" height="20"/>
              <span class="visually-hidden">{{ social.alt }}</span>
            </a>
          </li>
          {% endfor %}
        </ul>
        <small>
          <p>&copy; {{ helpers.currentYear() }} Ximena Vila Ferral</p>
          <p><a href="https://github.com/ximenavf92/ximenavf"><img src="/assets/img/github-icon-white.svg" alt="" width="10" height="10"><span class="visually-hidden">View this website's code on Github</span></a>&nbsp; Designed & coded with &hearts; by <a href="https://github.com/ximenavf92">Ximena Vf.</a> Built with <a href="https://www.11ty.dev/">Eleventy</a>. Hosted on <a href="https://netlify.com/">Netlify</a>.</p>
        </small>
      </footer>
    </siv>

    <!-- Current page: {{ page.url | url }} -->
  </body>
</html>